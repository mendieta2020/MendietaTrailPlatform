[pytest]
DJANGO_SETTINGS_MODULE = backend.settings
# Include both default and project-specific test file patterns.
python_files = test_*.py tests_*.py
# Recommended commands (PowerShell):
#   pytest --collect-only -q
#   pytest -q core/tests_planning.py -s
#   pytest -q core/tests_tenant_security.py -s
#   pytest -q core/tests_security_throttling.py -s
